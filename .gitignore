# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Environment files (secrets — never commit)
.env
.env.*
!.env.example
*.env

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
.playwright-ld-library-path

# Playwright JUnit output from the a11y CI gate
*.junit.xml

# Local-dev mastered-track fallback when SUPABASE_SERVICE_ROLE_KEY is unset
.data/

/src/generated/prisma

# Local Firecrawl scrape output
scraped-output.md

# Private background audio staging
.ingest_vault/
.audio_scratch/

# Free Music Archive metadata dump (local only)
data/fma/

# EP batch output (may contain R2 presigned URLs)
generated_ep_manifest.json
hybrid_studio.db

# Python bytecode caches
__pycache__/
*.pyc

# Generated DSP matrix cache (scripts/build_genre_matrix.py)
config/dsp_matrix.json
config/dsp_matrix_*.json

# Stem-role classifier artifacts and feature caches (ml/train_stem_classifier.py).
# The model is a couple of MB and the feature cache is tens of MB; both are
# reproducible from D:\MusicDatasets, so they stay out of git.
models/*.joblib
models/*.npz
models/*.onnx
ml/cache/

# Generated dataset manifests (scripts/build_dataset_manifest.py)
reports/dataset_manifest.sqlite
reports/dataset_manifest.jsonl

# Continuous learner checkpoints
models/checkpoints/
reports/drive_inventory.sqlite
