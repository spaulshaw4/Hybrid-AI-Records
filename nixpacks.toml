# Fallback Nixpacks plan (Railway prefers Dockerfile when present).
[phases.setup]
nixPkgs = ["bun", "ffmpeg", "python311"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build", "ls -la .output/server"]

[start]
cmd = "bun run .output/server/index.mjs"
